L1 Practice

# Day 04 - More Packages, Key Pairs, Service

## 🔹Ansible - Install Multiple Packages

---
- name: Install package for Pathnex
  hosts: all
  become: yes

  tasks:
    - name: Install tools
      yum:
        name:
          - git
          - wget
        status: present


        ## 🔹 Terraform - EC2 with Key Pair (c6i.9xlarge)

        provider "aws" {
            region = "us-east-1"
        }

        resource "aws_key_pair" "PathnexKey" {
            key_name = "PahtnexKey"
            public_key = "ssh-rsa AAA..."
        }

        resource "aws_instance" "PathnexEC2" {
            ami            = "ami-0abcd1234abcd1234"
            instancce_type = "c6.8xlarge"
            key_name       = aws_key_pair.PathnexKey_name

            tags = {
                Name ="Pathnex-EC2"
            }
        }


        ## 🔹 Kubernates - Create ClusterIP Service

        apiVersion: v1
        kind: Service
        metadata:
           name: pathnex-server
        spec: 
          selector:
            app: pathnex-app
          ports:
            - port: 80
              targetPort: 80


# Parallel Builds

## 🔹 Jenkins Pipeline - Parallel Build & Test

you will learn how to **run build and test in parallel**

pipeline{
    agent any
    stages {
        stages('Parallel Tasks') {
            parallel {
                stage('build') {
                    steps{
                        sh 'mvn clean compile'
                    }
                }
                stage('Test'){
                    steps {
                        sh 'mvn test'
                    }
                }
            }
        }
    }
}

## 🔹 GitLab CI - Parallel Jobs

you will learn how to **run parallel jobs in GitLab CI**

stages:
  - build-test

maven-build:
  stage: build-test
  image: maven:3.8.1-jdk-17
  script:
    - git clone https://github.com/Pathnex/sample-java-app.git
    - cd sample-java-app
    - mvn clean compile
  paralle: 1

  maven-test:
    stage: build-test
    image:maven:3.8.1-jdk-17
    script:
      - cd sample-java-app
      - mvn test
    parallel: 1


=======================================================================

L2 Practice:

Day 04 - Basic CI/CD Concept with Jenkins * GitLab


🔹 jenkinsfile - Basic CI Pipeline

pipeline {
      agent any
      stages {
          stage('Build') {
              steps {
                echo 'Building project'
              }
          }
          stage('Test') {
              steps{
                  echo 'Running tests'
              }
          }
          stage('Deploy') {
            steps {
                  echo 'Deploying application'
            }
          }
    }
}


🔹 GitLab CI/CD -Basic Pipeline

stages:
  - build 
  - test 
  - deploy

build:
    stage: build
    script: 
      - echo "Building tests"

test:
  stage: test 
  script: 
    - echo "Running tests"

deploy:
  stage: deploy
  script: 
    - echo "Deploying application"
    
    
🔹 Ansible - Install and Configure Nginx

- name: Install and Configure Nginx
  hosts: all 
  become: yes
  tasks:
    - name: Install Nginx
      yum:
        name: nginx 
        state: present 
    - name: Configure Nginx
      template:
        src: nginx.conf.j2 
        dest: /etc/nginx/nginx.conf 
    - name: Start nginx service
      service:
        name: nginx
        state: started
        enabled: yes


        🔹 Terraform - EC2 with  Security Group


resource "aws_security-group" "example" {
  name        = "example_security_group"
  description = "Allow SSH and HTTP"
  ingress {
    from-port   = 22
    to_port    = 22
    protocol   ="tcp"
    cidr_block = ["0.0.0.0/0"]
  }

  ingress {
    from_port  = 80
    to_port    = 80
    protocol   = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    from_port    = 0
    to_port      = 0
    protocol     = "-1"
    cidr_blocks  = ["0.0.0.0/0"]
  }
}


🔹 Docker

# Apache Container
FROM ununtu:22.04 
RUN apt update && apt install apache2 -y 
RUN echo "Hello Pathnex" > /var/www/html/index.html
CMD ["apachectl", "-D", "FOREGROUND"] 

🔹 Bash Real Pathnex

/var/www/html 

